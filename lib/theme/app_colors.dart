import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF3D51AD);
  static const Color secondary = Color(0xFF0D1D38);

  // Backgrounds
  // static const Color background = Color(0xFF201C2B);
  static const Color background = Color(0xFF272636);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Others
  static const Color error = Color(0xFFF44336);
  static const Color success = Color(0xFF3DAD52);
}
